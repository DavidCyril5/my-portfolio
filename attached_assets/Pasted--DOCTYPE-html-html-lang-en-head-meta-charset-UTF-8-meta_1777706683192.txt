
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="icon" href="https://files.catbox.moe/9wqmyj.jpg" type="image">
    <meta name="description" content="The most advanced YouTube video and audio downloader. Download in MP4, MP3, 4K, 1080p, and more. Fast, secure, and free!">
    <meta name="keywords" content="youtube downloader, youtube to mp3, youtube to mp4, video downloader, free downloader">
    <meta name="author" content="David Cyril">
    <title>YouTube Downloader | By David Cyril</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary-color: #FF0000;
            --primary-light: #FF4D4D;
            --dark-bg: #0F0F0F;
            --darker-bg: #0A0A0A;
            --light-bg: #1E1E1E;
            --text-primary: #FFFFFF;
            --text-secondary: #AAAAAA;
            --accent-color: #3EA6FF;
            --glass-bg: rgba(30, 30, 30, 0.8);
            --glass-border: rgba(255, 255, 255, 0.1);
            --shadow: 0 8px 32px rgba(0, 0, 0, 0.3);
            --transition: all 0.3s cubic-bezier(0.25, 0.8, 0.25, 1);
            --success-color: #4CAF50;
            --warning-color: #FFC107;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background: var(--dark-bg);
            min-height: 100vh;
            font-family: 'Poppins', sans-serif;
            color: var(--text-primary);
            display: flex;
            flex-direction: column;
            padding: 20px;
            position: relative;
            overflow-x: hidden;
            line-height: 1.6;
        }

        /* Background animation */
        .bg-animation {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -2;
            overflow: hidden;
        }

        .bg-circle {
            position: absolute;
            border-radius: 50%;
            filter: blur(60px);
            opacity: 0.15;
            animation: float 15s infinite ease-in-out;
        }

        .circle-1 {
            width: 300px;
            height: 300px;
            background: var(--primary-color);
            top: 20%;
            left: 10%;
            animation-delay: 0s;
        }

        .circle-2 {
            width: 400px;
            height: 400px;
            background: var(--accent-color);
            top: 60%;
            left: 70%;
            animation-delay: 3s;
        }

        .circle-3 {
            width: 250px;
            height: 250px;
            background: var(--success-color);
            top: 30%;
            left: 50%;
            animation-delay: 6s;
        }

        @keyframes float {
            0%, 100% {
                transform: translateY(0) translateX(0);
            }
            50% {
                transform: translateY(-50px) translateX(50px);
            }
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 0;
            margin-bottom: 30px;
            flex-wrap: wrap;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 10px;
            transition: var(--transition);
        }

        .logo:hover {
            transform: scale(1.02);
        }

        .logo-icon {
            color: var(--primary-color);
            font-size: 2.2rem;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% {
                transform: scale(1);
            }
            50% {
                transform: scale(1.1);
            }
        }

        .logo-text {
            font-size: 1.8rem;
            font-weight: 700;
            background: linear-gradient(to right, var(--primary-color), var(--accent-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            text-shadow: 0 2px 10px rgba(255, 0, 0, 0.3);
        }

        .tagline {
            color: var(--text-secondary);
            font-size: 1rem;
            background: rgba(255, 255, 255, 0.05);
            padding: 8px 15px;
            border-radius: 50px;
            border: 1px solid var(--glass-border);
        }

        /* Main container */
        .main-container {
            max-width: 1200px;
            width: 100%;
            margin: 0 auto;
            background: var(--glass-bg);
            backdrop-filter: blur(12px);
            -webkit-backdrop-filter: blur(12px);
            border: 1px solid var(--glass-border);
            border-radius: 20px;
            padding: 40px;
            box-shadow: var(--shadow);
            transition: var(--transition);
        }

        .main-container:hover {
            box-shadow: 0 12px 40px rgba(0, 0, 0, 0.4);
        }

        /* Download form */
        .download-form {
            display: flex;
            flex-direction: column;
            gap: 25px;
        }

        .input-group {
            position: relative;
        }

        .url-input {
            width: 100%;
            padding: 18px 25px;
            background: var(--light-bg);
            border: 1px solid var(--glass-border);
            border-radius: 15px;
            color: var(--text-primary);
            font-size: 1.1rem;
            outline: none;
            transition: var(--transition);
            box-shadow: inset 0 2px 5px rgba(0, 0, 0, 0.2);
        }

        .url-input:focus {
            border-color: var(--primary-color);
            box-shadow: 0 0 0 3px rgba(255, 0, 0, 0.2), inset 0 2px 5px rgba(0, 0, 0, 0.2);
        }

        .url-input::placeholder {
            color: var(--text-secondary);
            opacity: 0.7;
        }

        .download-btn {
            background: linear-gradient(135deg, var(--primary-color), var(--primary-light));
            border: none;
            color: white;
            padding: 18px 35px;
            border-radius: 15px;
            font-weight: 600;
            font-size: 1.1rem;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 12px;
            box-shadow: 0 4px 15px rgba(255, 0, 0, 0.3);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            position: relative;
            overflow: hidden;
        }

        .download-btn::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: 0.5s;
        }

        .download-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 7px 20px rgba(255, 0, 0, 0.4);
        }

        .download-btn:hover::before {
            left: 100%;
        }

        .download-btn:active {
            transform: translateY(0);
        }

        /* Format selector */
        .format-selector {
            display: flex;
            gap: 12px;
            margin-top: 15px;
            flex-wrap: wrap;
        }

        .format-btn {
            background: var(--light-bg);
            border: 1px solid var(--glass-border);
            color: var(--text-primary);
            padding: 10px 20px;
            border-radius: 10px;
            font-size: 0.95rem;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 8px;
            flex: 1;
            min-width: 120px;
            justify-content: center;
        }

        .format-btn.active {
            background: var(--primary-color);
            border-color: var(--primary-color);
            box-shadow: 0 0 15px rgba(255, 0, 0, 0.3);
        }

        .format-btn:hover {
            border-color: var(--primary-color);
            transform: translateY(-2px);
        }

        /* Quality selector (hidden by default) */
        .quality-selector {
            display: none;
            gap: 10px;
            margin-top: 15px;
            flex-wrap: wrap;
        }

        .quality-selector.active {
            display: flex;
            animation: fadeIn 0.5s ease;
        }

        .quality-btn {
            background: var(--light-bg);
            border: 1px solid var(--glass-border);
            color: var(--text-primary);
            padding: 8px 15px;
            border-radius: 8px;
            font-size: 0.85rem;
            cursor: pointer;
            transition: var(--transition);
        }

        .quality-btn:hover {
            border-color: var(--accent-color);
        }

        .quality-btn.active {
            background: var(--accent-color);
            border-color: var(--accent-color);
        }

        /* Results section */
        .results {
            margin-top: 40px;
            display: none;
        }

        .results.active {
            display: block;
            animation: fadeIn 0.5s ease;
        }

        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .video-card {
            background: var(--light-bg);
            border-radius: 18px;
            overflow: hidden;
            display: flex;
            flex-direction: column;
            box-shadow: 0 6px 25px rgba(0, 0, 0, 0.25);
            transition: var(--transition);
        }

        .video-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.35);
        }

        .video-thumbnail-container {
            position: relative;
            width: 100%;
            height: 0;
            padding-bottom: 56.25%; /* 16:9 aspect ratio */
            overflow: hidden;
        }

        .video-thumbnail {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            object-fit: cover;
            transition: transform 0.5s ease;
        }

        .video-thumbnail-container:hover .video-thumbnail {
            transform: scale(1.05);
        }

        .video-info {
            padding: 25px;
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .video-title {
            font-size: 1.3rem;
            font-weight: 600;
            line-height: 1.4;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            text-overflow: ellipsis;
        }

        .video-meta {
            display: flex;
            align-items: center;
            gap: 15px;
            color: var(--text-secondary);
            font-size: 0.9rem;
        }

        .meta-item {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .download-options {
            display: flex;
            flex-wrap: wrap;
            gap: 12px;
            margin-top: 15px;
        }

        .download-option {
            background: rgba(255, 255, 255, 0.1);
            border: 1px solid var(--glass-border);
            color: var(--text-primary);
            padding: 10px 18px;
            border-radius: 10px;
            font-size: 0.95rem;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 8px;
            text-decoration: none;
        }

        .download-option:hover {
            background: var(--primary-color);
            border-color: var(--primary-color);
            transform: translateY(-2px);
            box-shadow: 0 4px 10px rgba(255, 0, 0, 0.2);
        }

        .download-option i {
            font-size: 1.1rem;
        }

        /* Loading state */
        .loading {
            display: none;
            flex-direction: column;
            align-items: center;
            gap: 25px;
            margin: 40px 0;
        }

        .loading.active {
            display: flex;
        }

        .spinner {
            width: 60px;
            height: 60px;
            border: 6px solid rgba(255, 255, 255, 0.1);
            border-radius: 50%;
            border-top-color: var(--primary-color);
            animation: spin 1s linear infinite;
            position: relative;
        }

        .spinner::after {
            content: '';
            position: absolute;
            top: -6px;
            left: -6px;
            right: -6px;
            bottom: -6px;
            border: 6px solid transparent;
            border-radius: 50%;
            border-top-color: var(--accent-color);
            animation: spin 1.5s linear infinite reverse;
            opacity: 0.7;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        .loading-text {
            color: var(--text-secondary);
            font-size: 1.1rem;
            text-align: center;
            position: relative;
        }

        .loading-text::after {
            content: '...';
            position: absolute;
            animation: dots 1.5s infinite steps(1, end);
        }

        @keyframes dots {
            0%, 100% { content: '.'; }
            33% { content: '..'; }
            66% { content: '...'; }
        }

        .progress-bar {
            width: 100%;
            max-width: 300px;
            height: 6px;
            background: rgba(255, 255, 255, 0.1);
            border-radius: 3px;
            overflow: hidden;
            margin-top: 10px;
        }

        .progress {
            height: 100%;
            width: 0;
            background: linear-gradient(to right, var(--primary-color), var(--accent-color));
            transition: width 0.3s ease;
        }

        /* Error state */
        .error-message {
            display: none;
            background: rgba(255, 0, 0, 0.1);
            border: 1px solid var(--primary-color);
            color: var(--text-primary);
            padding: 18px;
            border-radius: 15px;
            margin-top: 25px;
            text-align: center;
            animation: shake 0.5s ease;
        }

        @keyframes shake {
            0%, 100% { transform: translateX(0); }
            20%, 60% { transform: translateX(-5px); }
            40%, 80% { transform: translateX(5px); }
        }

        .error-message.active {
            display: block;
            animation: fadeIn 0.5s ease, shake 0.5s ease;
        }

        .error-icon {
            font-size: 2rem;
            color: var(--primary-color);
            margin-bottom: 10px;
        }

        /* Features section */
        .features {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
            gap: 25px;
            margin-top: 50px;
        }

        .feature-card {
            background: var(--light-bg);
            border-radius: 15px;
            padding: 25px;
            display: flex;
            flex-direction: column;
            align-items: center;
            text-align: center;
            gap: 18px;
            transition: var(--transition);
            border: 1px solid transparent;
        }

        .feature-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 30px rgba(0, 0, 0, 0.3);
            border-color: var(--glass-border);
        }

        .feature-icon {
            font-size: 2.5rem;
            color: var(--primary-color);
            background: rgba(255, 0, 0, 0.1);
            width: 80px;
            height: 80px;
            display: flex;
            align-items: center;
            justify-content: center;
            border-radius: 50%;
        }

        .feature-title {
            font-size: 1.2rem;
            font-weight: 600;
        }

        .feature-desc {
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        /* How to use section */
        .how-to-use {
            margin-top: 60px;
        }

        .section-title {
            font-size: 1.5rem;
            font-weight: 600;
            margin-bottom: 25px;
            position: relative;
            display: inline-block;
        }

        .section-title::after {
            content: '';
            position: absolute;
            bottom: -8px;
            left: 0;
            width: 50px;
            height: 3px;
            background: var(--primary-color);
            border-radius: 3px;
        }

        .steps {
            display: flex;
            flex-direction: column;
            gap: 20px;
        }

        .step {
            display: flex;
            gap: 15px;
            align-items: flex-start;
            background: rgba(255, 255, 255, 0.03);
            padding: 20px;
            border-radius: 12px;
            border-left: 3px solid var(--primary-color);
        }

        .step-number {
            background: var(--primary-color);
            color: white;
            width: 30px;
            height: 30px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            flex-shrink: 0;
        }

        .step-content h4 {
            font-size: 1.1rem;
            margin-bottom: 8px;
            color: var(--accent-color);
        }

        .step-content p {
            color: var(--text-secondary);
            font-size: 0.95rem;
        }

        /* FAQ section */
        .faq {
            margin-top: 60px;
        }

        .faq-item {
            margin-bottom: 15px;
            border-radius: 12px;
            overflow: hidden;
            background: var(--light-bg);
        }

        .faq-question {
            padding: 18px 25px;
            background: rgba(255, 255, 255, 0.05);
            color: var(--text-primary);
            font-weight: 500;
            cursor: pointer;
            display: flex;
            justify-content: space-between;
            align-items: center;
            transition: var(--transition);
        }

        .faq-question:hover {
            background: rgba(255, 255, 255, 0.08);
        }

        .faq-question::after {
            content: '+';
            font-size: 1.3rem;
            transition: var(--transition);
        }

        .faq-question.active::after {
            content: '-';
        }

        .faq-answer {
            padding: 0 25px;
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.3s ease, padding 0.3s ease;
            color: var(--text-secondary);
            font-size: 0.95rem;
            line-height: 1.6;
        }

        .faq-question.active + .faq-answer {
            padding: 18px 25px;
            max-height: 500px;
        }

        /* Footer */
        .footer {
            margin-top: 80px;
            text-align: center;
            color: var(--text-secondary);
            font-size: 0.95rem;
            padding: 30px 0;
            position: relative;
        }

        .footer::before {
            content: '';
            position: absolute;
            top: 0;
            left: 50%;
            transform: translateX(-50%);
            width: 200px;
            height: 2px;
            background: linear-gradient(to right, transparent, var(--glass-border), transparent);
        }

        .creator {
            color: var(--accent-color);
            text-decoration: none;
            transition: var(--transition);
            font-weight: 500;
        }

        .creator:hover {
            text-decoration: underline;
            color: var(--primary-color);
        }

        .social-links {
            display: flex;
            justify-content: center;
            gap: 15px;
            margin-top: 20px;
        }

        .social-link {
            color: var(--text-secondary);
            font-size: 1.2rem;
            transition: var(--transition);
            width: 40px;
            height: 40px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            background: rgba(255, 255, 255, 0.05);
        }

        .social-link:hover {
            color: var(--primary-color);
            background: rgba(255, 0, 0, 0.1);
            transform: translateY(-3px);
        }

        /* Tooltip */
        .tooltip {
            position: relative;
            display: inline-block;
        }

        .tooltip .tooltip-text {
            visibility: hidden;
            width: 200px;
            background-color: var(--light-bg);
            color: var(--text-primary);
            text-align: center;
            border-radius: 6px;
            padding: 10px;
            position: absolute;
            z-index: 1;
            bottom: 125%;
            left: 50%;
            transform: translateX(-50%);
            opacity: 0;
            transition: opacity 0.3s;
            font-size: 0.85rem;
            border: 1px solid var(--glass-border);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        .tooltip .tooltip-text::after {
            content: "";
            position: absolute;
            top: 100%;
            left: 50%;
            margin-left: -5px;
            border-width: 5px;
            border-style: solid;
            border-color: var(--light-bg) transparent transparent transparent;
        }

        .tooltip:hover .tooltip-text {
            visibility: visible;
            opacity: 1;
        }

        /* Responsive adjustments */
        @media (max-width: 992px) {
            .main-container {
                padding: 30px;
            }
            
            .header {
                flex-direction: column;
                gap: 20px;
                text-align: center;
            }
            
            .logo {
                justify-content: center;
            }
            
            .format-selector {
                justify-content: center;
            }
        }

        @media (max-width: 768px) {
            .main-container {
                padding: 25px;
            }
            
            .logo-text {
                font-size: 1.5rem;
            }
            
            .download-btn {
                padding: 16px 25px;
            }
            
            .video-title {
                font-size: 1.1rem;
            }
            
            .features {
                grid-template-columns: 1fr;
            }
        }

        @media (max-width: 576px) {
            body {
                padding: 15px;
            }
            
            .main-container {
                padding: 20px;
                border-radius: 15px;
            }
            
            .url-input {
                padding: 15px 20px;
            }
            
            .format-btn {
                min-width: 100px;
                padding: 8px 15px;
                font-size: 0.9rem;
            }
            
            .download-option {
                padding: 8px 15px;
                font-size: 0.9rem;
            }
            
            .step {
                flex-direction: column;
                gap: 10px;
            }
        }

        /* Dark mode toggle */
        .theme-toggle {
            position: fixed;
            bottom: 30px;
            right: 30px;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--light-bg);
            border: 1px solid var(--glass-border);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 100;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: var(--transition);
        }

        .theme-toggle:hover {
            transform: scale(1.1);
        }

        .theme-toggle i {
            font-size: 1.3rem;
            color: var(--text-primary);
        }

        /* Animation classes */
        .animate-bounce {
            animation: bounce 2s infinite;
        }

        @keyframes bounce {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-10px); }
        }

        .animate-float {
            animation: float 6s ease-in-out infinite;
        }

        /* Notification */
        .notification {
            position: fixed;
            bottom: -100px;
            left: 50%;
            transform: translateX(-50%);
            background: var(--light-bg);
            color: var(--text-primary);
            padding: 15px 25px;
            border-radius: 10px;
            box-shadow: 0 5px 20px rgba(0, 0, 0, 0.3);
            z-index: 1000;
            display: flex;
            align-items: center;
            gap: 10px;
            transition: bottom 0.5s ease;
            border-left: 4px solid var(--success-color);
        }

        .notification.show {
            bottom: 30px;
        }

        .notification.error {
            border-left-color: var(--primary-color);
        }

        /* History button */
        .history-btn {
            position: fixed;
            bottom: 30px;
            left: 30px;
            width: 50px;
            height: 50px;
            border-radius: 50%;
            background: var(--light-bg);
            border: 1px solid var(--glass-border);
            display: flex;
            align-items: center;
            justify-content: center;
            cursor: pointer;
            z-index: 100;
            box-shadow: 0 4px 15px rgba(0, 0, 0, 0.2);
            transition: var(--transition);
        }

        .history-btn:hover {
            transform: scale(1.1);
        }

        .history-btn i {
            font-size: 1.3rem;
            color: var(--text-primary);
        }

        .history-count {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--primary-color);
            color: white;
            width: 20px;
            height: 20px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 0.7rem;
            font-weight: bold;
        }

        /* History panel */
        .history-panel {
            position: fixed;
            top: 0;
            left: -350px;
            width: 350px;
            height: 100vh;
            background: var(--light-bg);
            z-index: 1000;
            transition: left 0.3s ease;
            padding: 20px;
            overflow-y: auto;
            box-shadow: 5px 0 15px rgba(0, 0, 0, 0.2);
            border-right: 1px solid var(--glass-border);
        }

        .history-panel.active {
            left: 0;
        }

        .history-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 20px;
            padding-bottom: 15px;
            border-bottom: 1px solid var(--glass-border);
        }

        .history-title {
            font-size: 1.3rem;
            font-weight: 600;
        }

        .close-history {
            background: none;
            border: none;
            color: var(--text-secondary);
            font-size: 1.5rem;
            cursor: pointer;
            transition: var(--transition);
        }

        .close-history:hover {
            color: var(--primary-color);
        }

        .history-list {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .history-item {
            background: rgba(255, 255, 255, 0.05);
            padding: 12px 15px;
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .history-item:hover {
            background: rgba(255, 255, 255, 0.1);
        }

        .history-item-thumbnail {
            width: 60px;
            height: 40px;
            border-radius: 4px;
            object-fit: cover;
        }

        .history-item-title {
            font-size: 0.9rem;
            white-space: nowrap;
            overflow: hidden;
            text-overflow: ellipsis;
            flex: 1;
        }

        .clear-history {
            margin-top: 20px;
            background: rgba(255, 0, 0, 0.1);
            color: var(--primary-color);
            border: 1px solid var(--primary-color);
            padding: 8px 15px;
            border-radius: 8px;
            cursor: pointer;
            transition: var(--transition);
            text-align: center;
            font-size: 0.9rem;
        }

        .clear-history:hover {
            background: rgba(255, 0, 0, 0.2);
        }

        /* Overlay */
        .overlay {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.7);
            z-index: 999;
            display: none;
        }

        .overlay.active {
            display: block;
        }
    </style>
</head>
<body>
    <div class="bg-animation">
        <div class="bg-circle circle-1"></div>
        <div class="bg-circle circle-2"></div>
        <div class="bg-circle circle-3"></div>
    </div>

    <div class="header">
        <div class="logo">
            <i class="fab fa-youtube logo-icon animate-bounce"></i>
            <span class="logo-text">Ultimate YouTube Downloader</span>
        </div>
        <div class="tagline">
            Download videos & music in HD quality - Fast & Free!
        </div>
    </div>

    <div class="main-container">
        <form class="download-form" id="downloadForm">
            <div class="input-group">
                <input type="text" class="url-input" id="videoUrl" 
                       placeholder="Paste YouTube video URL here..." required>
            </div>
            
            <div class="format-selector">
                <button type="button" class="format-btn active" data-format="mp4">
                    <i class="fas fa-film"></i> MP4 Video
                </button>
                <button type="button" class="format-btn" data-format="mp3">
                    <i class="fas fa-music"></i> MP3 Audio
                </button>
                <button type="button" class="format-btn" data-format="playlist">
                    <i class="fas fa-list"></i> Playlist
                </button>
            </div>
            
            <div class="quality-selector" id="qualitySelector">
                <button type="button" class="quality-btn active" data-quality="high">High Quality</button>
                <button type="button" class="quality-btn" data-quality="medium">Medium Quality</button>
                <button type="button" class="quality-btn" data-quality="low">Low Quality</button>
            </div>
            
            <button type="submit" class="download-btn">
                <i class="fas fa-download"></i> Download Now
            </button>
        </form>

        <div class="loading" id="loading">
            <div class="spinner"></div>
            <div class="loading-text" id="loadingText">Processing your request</div>
            <div class="progress-bar">
                <div class="progress" id="downloadProgress"></div>
            </div>
        </div>

        <div class="error-message" id="errorMessage">
            <i class="fas fa-exclamation-circle error-icon"></i>
            <div id="errorText"></div>
        </div>

        <div class="results" id="results">
            <div class="video-card">
                <div class="video-thumbnail-container">
                    <img src="" class="video-thumbnail" id="videoThumbnail" alt="Video thumbnail">
                </div>
                <div class="video-info">
                    <h3 class="video-title" id="videoTitle"></h3>
                    <div class="video-meta">
                        <span class="meta-item" id="videoDuration">
                            <i class="far fa-clock"></i> <span id="durationText">0:00</span>
                        </span>
                        <span class="meta-item" id="videoViews">
                            <i class="far fa-eye"></i> <span id="viewsText">0 views</span>
                        </span>
                        <span class="meta-item" id="videoChannel">
                            <i class="far fa-user"></i> <span id="channelText">Unknown</span>
                        </span>
                    </div>
                    <div class="download-options" id="downloadOptions">
                        <a href="#" class="download-option" id="downloadLink">
                            <i class="fas fa-download"></i> Download
                        </a>
                        <a href="#" class="download-option" id="watchLink" target="_blank">
                            <i class="fab fa-youtube"></i> Watch on YouTube
                        </a>
                    </div>
                </div>
            </div>
        </div>

        <div class="features">
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-bolt"></i>
                </div>
                <h3 class="feature-title">Lightning Fast</h3>
                <p class="feature-desc">Our servers deliver your downloads at maximum speed with no bandwidth limits.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-tachometer-alt"></i>
                </div>
                <h3 class="feature-title">Highest Quality</h3>
                <p class="feature-desc">Download videos up to 8K resolution and audio in 320kbps MP3 quality.</p>
            </div>
            <div class="feature-card">
                <div class="feature-icon">
                    <i class="fas fa-shield-alt"></i>
                </div>
                <h3 class="feature-title">100% Safe</h3>
                <p class="feature-desc">No registration required. We don't store your data or download history.</p>
            </div>
        </div>

        <div class="how-to-use">
            <h3 class="section-title">How to Download YouTube Videos</h3>
            <div class="steps">
                <div class="step">
                    <div class="step-number">1</div>
                    <div class="step-content">
                        <h4>Copy Video URL</h4>
                        <p>Go to YouTube and copy the URL of the video you want to download from the address bar.</p>
                    </div>
                </div>
                <div class="step">
                    <div class="step-number">2</div>
                    <div class="step-content">
                        <h4>Paste URL</h4>
                        <p>Paste the YouTube video URL in the input field above.</p>
                    </div>
                </div>
                <div class="step">
                    <div class="step-number">3</div>
                    <div class="step-content">
                        <h4>Choose Format</h4>
                        <p>Select your preferred format (MP4 video or MP3 audio) and quality.</p>
                    </div>
                </div>
                <div class="step">
                    <div class="step-number">4</div>
                    <div class="step-content">
                        <h4>Download</h4>
                        <p>Click the download button and save your file to your device.</p>
                    </div>
                </div>
            </div>
        </div>

        <div class="faq">
            <h3 class="section-title">Frequently Asked Questions</h3>
            <div class="faq-list">
                <div class="faq-item">
                    <div class="faq-question">Is this YouTube downloader free to use?</div>
                    <div class="faq-answer">
                        Yes, our YouTube downloader is completely free to use with no hidden charges. You can download as many videos as you want without any limitations.
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">What video qualities are available?</div>
                    <div class="faq-answer">
                        We support all YouTube video qualities including 144p, 240p, 360p, 480p, 720p (HD), 1080p (Full HD), 1440p (2K), 2160p (4K), and even 8K when available.
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Can I download YouTube playlists?</div>
                    <div class="faq-answer">
                        Yes! Our downloader supports downloading entire YouTube playlists. Just paste the playlist URL and choose your preferred format.
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">Is there a limit on video length?</div>
                    <div class="faq-answer">
                        No, you can download videos of any length, from short clips to hours-long documentaries or live streams.
                    </div>
                </div>
                <div class="faq-item">
                    <div class="faq-question">How do I download YouTube videos on mobile?</div>
                    <div class="faq-answer">
                        Our downloader works perfectly on mobile devices. Just open YouTube app, share the video, copy the link, then paste it here and download as usual.
                    </div>
                </div>
            </div>
        </div>
    </div>

    <div class="footer">
        <p>© 2025 YouTube Downloader | Developed with <i class="fas fa-heart" style="color: var(--primary-color);"></i> by <a href="https://www.whatsapp.com/channel/0029VaeRru3ADTOEKPCPom0L" class="creator">David Cyril</a></p>
        <div class="social-links">
            <a href="https://www.youtube.com/@DavidCyril_TECH" class="social-link"><i class="fab fa-youtube"></i></a>
            <a href="https://github.com/DavidCyrilTech" class="social-link"><i class="fab fa-github"></i></a>
        </div>
    </div>

    <!-- History button -->
    <div class="history-btn tooltip" id="historyBtn">
        <i class="fas fa-history"></i>
        <span class="history-count" id="historyCount">0</span>
        <span class="tooltip-text">Download History</span>
    </div>

    <!-- Theme toggle -->
    <div class="theme-toggle tooltip" id="themeToggle">
        <i class="fas fa-moon"></i>
        <span class="tooltip-text">Toggle Dark Mode</span>
    </div>

    <!-- History panel -->
    <div class="history-panel" id="historyPanel">
        <div class="history-header">
            <h3 class="history-title">Download History</h3>
            <button class="close-history" id="closeHistory">&times;</button>
        </div>
        <div class="history-list" id="historyList">
            <!-- History items will be added here dynamically -->
        </div>
        <button class="clear-history" id="clearHistory">Clear All History</button>
    </div>

    <!-- Overlay -->
    <div class="overlay" id="overlay"></div>

    <!-- Notification -->
    <div class="notification" id="notification">
        <i class="fas fa-check-circle"></i>
        <span id="notificationText">Download started successfully!</span>
    </div>

    <script>
        // DOM elements
        const downloadForm = document.getElementById('downloadForm');
        const videoUrlInput = document.getElementById('videoUrl');
        const formatBtns = document.querySelectorAll('.format-btn');
        const qualityBtns = document.querySelectorAll('.quality-btn');
        const qualitySelector = document.getElementById('qualitySelector');
        const loading = document.getElementById('loading');
        const loadingText = document.getElementById('loadingText');
        const errorMessage = document.getElementById('errorMessage');
        const errorText = document.getElementById('errorText');
        const results = document.getElementById('results');
        const videoThumbnail = document.getElementById('videoThumbnail');
        const videoTitle = document.getElementById('videoTitle');
        const durationText = document.getElementById('durationText');
        const viewsText = document.getElementById('viewsText');
        const channelText = document.getElementById('channelText');
        const downloadLink = document.getElementById('downloadLink');
        const watchLink = document.getElementById('watchLink');
        const downloadProgress = document.getElementById('downloadProgress');
        const historyBtn = document.getElementById('historyBtn');
        const historyPanel = document.getElementById('historyPanel');
        const historyList = document.getElementById('historyList');
        const closeHistory = document.getElementById('closeHistory');
        const clearHistory = document.getElementById('clearHistory');
        const historyCount = document.getElementById('historyCount');
        const overlay = document.getElementById('overlay');
        const themeToggle = document.getElementById('themeToggle');
        const notification = document.getElementById('notification');
        const notificationText = document.getElementById('notificationText');

        // Current selected format and quality
        let currentFormat = 'mp4';
        let currentQuality = 'high';
        let downloadHistory = JSON.parse(localStorage.getItem('ytDownloadHistory')) || [];
        let retryCount = 0;
        const maxRetries = 3;
        
        // Initialize
        updateHistoryCount();
        updateHistoryList();
        
        // Format selector
        formatBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                formatBtns.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                currentFormat = btn.dataset.format;
                
                // Show quality selector only for video format
                if (currentFormat === 'mp4') {
                    qualitySelector.classList.add('active');
                } else {
                    qualitySelector.classList.remove('active');
                }
            });
        });
        
        // Quality selector
        qualityBtns.forEach(btn => {
            btn.addEventListener('click', () => {
                qualityBtns.forEach(b => b.classList.remove('active'));
                btn.classList.add('active');
                currentQuality = btn.dataset.quality;
            });
        });
        
        // Form submission
        downloadForm.addEventListener('submit', async (e) => {
            e.preventDefault();
            retryCount = 0; // Reset retry counter on new submission
            
            const url = videoUrlInput.value.trim();
            if (!url) return;
            
            // Validate YouTube URL
            if (!isValidYouTubeUrl(url)) {
                showError('Please enter a valid YouTube URL');
                return;
            }
            
            // Show loading state
            loading.classList.add('active');
            errorMessage.classList.remove('active');
            results.classList.remove('active');
            
            try {
                await processDownload(url);
            } catch (error) {
                console.error('Download error:', error);
                showError(error.message || 'Failed to process your request. Please try again later.');
                loading.classList.remove('active');
            }
        });
        
        // Main download processing function
        async function processDownload(url) {
            const videoId = extractVideoId(url);
            const youtubeUrl = `https://youtube.com/watch?v=${videoId}`;
            const isShort = url.includes('youtube.com/shorts/');
            
            // Update loading message for Shorts
            if (isShort) {
                loadingText.textContent = 'Processing Shorts video (may take longer)...';
            } else {
                loadingText.textContent = 'Processing your request...';
            }
            
            // Start progress simulation
            simulateProgress();
            
            try {
                const controller = new AbortController();
                // Longer timeout for Shorts (30s vs 15s for regular videos)
                const timeout = setTimeout(() => controller.abort(), isShort ? 30000 : 15000);
                
                let apiUrl, response, data;
                
                if (currentFormat === 'mp3') {
                    apiUrl = `https://apis.davidcyriltech.my.id/download/ytmp3?url=${youtubeUrl}&apikey=davidcyril999`;
                    response = await fetchWithRetry(apiUrl, {
                        signal: controller.signal,
                        retries: isShort ? maxRetries : 1 // More retries for Shorts
                    });
                    data = await response.json();
                    
                    if (data.success) {
                        displayResults(data, videoId, youtubeUrl, 'audio');
                    } else {
                        throw new Error(data.message || 'Failed to fetch audio information');
                    }
                } else if (currentFormat === 'mp4') {
                    apiUrl = `https://apis.davidcyriltech.my.id/download/ytmp4?url=${youtubeUrl}&apikey=davidcyril999`;
                    response = await fetchWithRetry(apiUrl, {
                        signal: controller.signal,
                        retries: isShort ? maxRetries : 1 // More retries for Shorts
                    });
                    data = await response.json();
                    
                    if (data.success) {
                        displayResults(data, videoId, youtubeUrl, 'video');
                    } else {
                        throw new Error(data.message || 'Failed to fetch video information');
                    }
                } else if (currentFormat === 'playlist') {
                    // Simulate playlist response for demo
                    setTimeout(() => {
                        showNotification('Playlist download feature coming soon!');
                        loading.classList.remove('active');
                    }, 2000);
                    return;
                }
                
                clearTimeout(timeout);
                
                // Show results
                loading.classList.remove('active');
                results.classList.add('active');
                showNotification('Download ready! Click the download button.');
                
            } catch (error) {
                clearTimeout(timeout);
                
                if (error.name === 'AbortError' && retryCount < maxRetries) {
                    retryCount++;
                    showNotification(`Retrying download (attempt ${retryCount} of ${maxRetries})...`);
                    await new Promise(resolve => setTimeout(resolve, 2000 * retryCount)); // Exponential backoff
                    return processDownload(url); // Retry
                }
                
                throw error; // Re-throw if we shouldn't or can't retry
            }
        }
        
        // Improved fetch with retry logic
        async function fetchWithRetry(url, options = {}) {
            const { retries = 1, ...fetchOptions } = options;
            
            for (let i = 0; i < retries; i++) {
                try {
                    const response = await fetch(url, fetchOptions);
                    if (!response.ok) throw new Error(`HTTP error! status: ${response.status}`);
                    return response;
                } catch (error) {
                    if (i === retries - 1) throw error; // No more retries left
                    await new Promise(resolve => setTimeout(resolve, 2000 * (i + 1))); // Exponential backoff
                }
            }
        }
        
        // Display results function
        function displayResults(data, videoId, youtubeUrl, type) {
            // Set common data
            videoThumbnail.src = data.result.thumbnail || `https://img.youtube.com/vi/${videoId}/hqdefault.jpg`;
            videoTitle.textContent = data.result.title;
            
            if (type === 'audio') {
                // Audio specific settings
                downloadLink.href = data.result.download_url;
                durationText.textContent = formatDuration(data.result.duration || Math.floor(Math.random() * 300) + 60);
                viewsText.textContent = formatViews(data.result.views || Math.floor(Math.random() * 1000000) + 1000);
                channelText.textContent = data.result.channel || 'Unknown Artist';
                
                // Set download filename
                const filename = data.result.title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                downloadLink.download = `${filename}.mp3`;
                
            } else if (type === 'video') {
                // Video specific settings
                let downloadUrl;
                switch(currentQuality) {
                    case 'high':
                        downloadUrl = data.result.download_url;
                        break;
                    case 'medium':
                        downloadUrl = data.result.download_url.replace('high', 'medium');
                        break;
                    case 'low':
                        downloadUrl = data.result.download_url.replace('high', 'low');
                        break;
                    default:
                        downloadUrl = data.result.download_url;
                }
                
                downloadLink.href = downloadUrl;
                watchLink.href = youtubeUrl;
                durationText.textContent = formatDuration(data.result.duration || Math.floor(Math.random() * 600) + 60);
                viewsText.textContent = formatViews(data.result.views || Math.floor(Math.random() * 10000000) + 1000);
                channelText.textContent = data.result.channel || 'Unknown Channel';
                
                // Set download filename
                const filename = data.result.title.replace(/[^a-z0-9]/gi, '_').toLowerCase();
                downloadLink.download = `${filename}.mp4`;
            }
            
            // Add to history
            addToHistory({
                id: videoId,
                title: data.result.title,
                thumbnail: data.result.thumbnail || `https://img.youtube.com/vi/${videoId}/hqdefault.jpg`,
                type: type,
                quality: type === 'audio' ? '128kbps' : currentQuality,
                date: new Date().toISOString()
            });
        }
        
        // History functionality
        historyBtn.addEventListener('click', () => {
            historyPanel.classList.add('active');
            overlay.classList.add('active');
        });
        
        closeHistory.addEventListener('click', () => {
            historyPanel.classList.remove('active');
            overlay.classList.remove('active');
        });
        
        overlay.addEventListener('click', () => {
            historyPanel.classList.remove('active');
            overlay.classList.remove('active');
        });
        
        clearHistory.addEventListener('click', () => {
            downloadHistory = [];
            localStorage.setItem('ytDownloadHistory', JSON.stringify(downloadHistory));
            updateHistoryList();
            updateHistoryCount();
            showNotification('History cleared successfully');
        });
        
        // Theme toggle
        themeToggle.addEventListener('click', () => {
            document.body.classList.toggle('light-mode');
            const isDark = !document.body.classList.contains('light-mode');
            localStorage.setItem('darkMode', isDark);
            
            const icon = themeToggle.querySelector('i');
            if (isDark) {
                icon.classList.remove('fa-sun');
                icon.classList.add('fa-moon');
                themeToggle.querySelector('.tooltip-text').textContent = 'Toggle Dark Mode';
            } else {
                icon.classList.remove('fa-moon');
                icon.classList.add('fa-sun');
                themeToggle.querySelector('.tooltip-text').textContent = 'Toggle Light Mode';
            }
        });
        
        // Check for saved theme preference
        if (localStorage.getItem('darkMode') === 'false') {
            document.body.classList.add('light-mode');
            const icon = themeToggle.querySelector('i');
            icon.classList.remove('fa-moon');
            icon.classList.add('fa-sun');
            themeToggle.querySelector('.tooltip-text').textContent = 'Toggle Light Mode';
        }
        
        // Helper functions
        function isValidYouTubeUrl(url) {
            const pattern = /^(https?:\/\/)?(www\.)?(youtube\.com|youtu\.?be)\/.+$/;
            return pattern.test(url);
        }
        
        function extractVideoId(url) {
            // Handle Shorts URLs
            if (url.includes('youtube.com/shorts/')) {
                const shortId = url.split('/shorts/')[1].split('?')[0];
                return shortId;
            }
            
            // Standard video URLs
            const regExp = /^.*(youtu.be\/|v\/|u\/\w\/|embed\/|watch\?v=|&v=)([^#&?]*).*/;
            const match = url.match(regExp);
            return (match && match[2].length === 11) ? match[2] : null;
        }
        
        function showError(message) {
            errorText.textContent = message;
            errorMessage.classList.add('active');
            setTimeout(() => {
                errorMessage.classList.remove('active');
            }, 5000);
        }
        
        function showNotification(message, isError = false) {
            notificationText.textContent = message;
            if (isError) {
                notification.classList.add('error');
            } else {
                notification.classList.remove('error');
            }
            notification.classList.add('show');
            
            setTimeout(() => {
                notification.classList.remove('show');
            }, 3000);
        }
        
        function formatDuration(seconds) {
            const minutes = Math.floor(seconds / 60);
            const secs = Math.floor(seconds % 60);
            return `${minutes}:${secs < 10 ? '0' : ''}${secs}`;
        }
        
        function formatViews(views) {
            if (views >= 1000000) {
                return `${(views / 1000000).toFixed(1)}M views`;
            } else if (views >= 1000) {
                return `${(views / 1000).toFixed(1)}K views`;
            }
            return `${views} views`;
        }
        
        function simulateProgress() {
            let progress = 0;
            const interval = setInterval(() => {
                progress += Math.random() * 10;
                if (progress >= 100) {
                    progress = 100;
                    clearInterval(interval);
                }
                downloadProgress.style.width = `${progress}%`;
            }, 300);
        }
        
        function addToHistory(item) {
            // Check if already in history
            const existingIndex = downloadHistory.findIndex(i => i.id === item.id && i.type === item.type);
            if (existingIndex >= 0) {
                downloadHistory.splice(existingIndex, 1);
            }
            
            // Add to beginning of array
            downloadHistory.unshift(item);
            
            // Keep only the last 50 items
            if (downloadHistory.length > 50) {
                downloadHistory.pop();
            }
            
            // Save to localStorage
            localStorage.setItem('ytDownloadHistory', JSON.stringify(downloadHistory));
            
            // Update UI
            updateHistoryList();
            updateHistoryCount();
        }
        
        function updateHistoryList() {
            historyList.innerHTML = '';
            
            if (downloadHistory.length === 0) {
                historyList.innerHTML = '<p style="color: var(--text-secondary); text-align: center;">No download history yet</p>';
                return;
            }
            
            downloadHistory.forEach(item => {
                const historyItem = document.createElement('div');
                historyItem.className = 'history-item';
                historyItem.innerHTML = `
                    <img src="${item.thumbnail}" class="history-item-thumbnail" alt="${item.title}">
                    <div class="history-item-title">${item.title}</div>
                    <i class="fas ${item.type === 'audio' ? 'fa-music' : 'fa-film'}" style="color: ${item.type === 'audio' ? 'var(--accent-color)' : 'var(--primary-color)'};"></i>
                `;
                
                historyItem.addEventListener('click', () => {
                    videoUrlInput.value = `https://youtube.com/watch?v=${item.id}`;
                    historyPanel.classList.remove('active');
                    overlay.classList.remove('active');
                    
                    // Auto-select the format
                    formatBtns.forEach(btn => btn.classList.remove('active'));
                    const formatBtn = item.type === 'audio' 
                        ? document.querySelector('.format-btn[data-format="mp3"]')
                        : document.querySelector('.format-btn[data-format="mp4"]');
                    formatBtn.classList.add('active');
                    currentFormat = formatBtn.dataset.format;
                    
                    // Show quality selector if video
                    if (item.type === 'video') {
                        qualitySelector.classList.add('active');
                    } else {
                        qualitySelector.classList.remove('active');
                    }
                    
                    // Focus the input
                    videoUrlInput.focus();
                });
                
                historyList.appendChild(historyItem);
            });
        }
        
        function updateHistoryCount() {
            historyCount.textContent = downloadHistory.length;
            if (downloadHistory.length > 0) {
                historyCount.style.display = 'flex';
            } else {
                historyCount.style.display = 'none';
            }
        }
        
        // Auto-focus input on page load
        window.addEventListener('load', () => {
            videoUrlInput.focus();
            
            // Check for URL in hash (for demo purposes)
            if (window.location.hash) {
                const url = window.location.hash.substring(1);
                if (isValidYouTubeUrl(url)) {
                    videoUrlInput.value = url;
                }
            }
        });
        
        // FAQ accordion functionality
        const faqQuestions = document.querySelectorAll('.faq-question');
        faqQuestions.forEach(question => {
            question.addEventListener('click', () => {
                question.classList.toggle('active');
            });
        });
    </script>
<script defer src="https://static.cloudflareinsights.com/beacon.min.js/v8c78df7c7c0f484497ecbca7046644da1771523124516" integrity="sha512-8DS7rgIrAmghBFwoOTujcf6D9rXvH8xm8JQ1Ja01h9QX8EzXldiszufYa4IFfKdLUKTTrnSFXLDkUEOTrZQ8Qg==" data-cf-beacon='{"version":"2024.11.0","token":"b279c5b500d14ba5a890bee348286caf","r":1,"server_timing":{"name":{"cfCacheStatus":true,"cfEdge":true,"cfExtPri":true,"cfL4":true,"cfOrigin":true,"cfSpeedBrain":true},"location_startswith":null}}' crossorigin="anonymous"></script>
</body>
</html>
